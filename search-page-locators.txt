
  await page.goto('http://localhost:3000/search');
  await page.getByRole('searchbox', { name: 'Search venues' }).click();
  await page.getByRole('searchbox', { name: 'Search venues' }).fill('ka');
  await page.getByRole('button', { name: 'Search' }).click();
  await page.getByRole('button', { name: 'Select cities' }).click();
  await page.getByRole('menuitemcheckbox', { name: 'Bangkok' }).click();
  await page.getByRole('button', { name: 'Select workspace types' }).click();
  await page.getByLabel('Select workspace types').getByText('Accommodation').click();
  await page.getByRole('button', { name: 'Select amenities' }).click();
  await page.getByLabel('Select amenities').getByText('Business Center Available').click();
  await page.getByRole('button', { name: 'Search' }).click();
  await page.getByRole('button', { name: 'Search' }).click();

  Page 2: When search return no result >

  getByText('Failed to load search results')
  getByTestId('search-retry-button')
  getByText('Error:')
